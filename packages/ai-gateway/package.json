{
  "name": "@biina/ai-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BIINA AI Gateway — framework-agnostic provider abstraction (server-side only). Phase 1: types + registry + mock provider. Phase 2 adds Ollama and OpenAI-compatible providers.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
